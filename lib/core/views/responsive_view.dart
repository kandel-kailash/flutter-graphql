import 'package:flutter/widgets.dart';

interface class ResponsiveView {
  factory ResponsiveView.responsive(BuildContext context) =>
      throw UnimplementedError('Responsive factory must be implemented');
}
